version: '3.8'

services:
  rucord:
    build: .
    ports:
      - "9001:3001"
    volumes:
      - rucord-data:/app/server/data
    environment:
      - DB_PATH=/app/server/data/rucord.db
    restart: unless-stopped

volumes:
  rucord-data:
